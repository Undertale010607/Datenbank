# Datenbank

Dieses kleine Java-Programm bietet eine einfache SQLite-basierte Adressdatenbank mit CRUD-Funktionen.

Kurzbeschreibung
- Erzeugt und verwaltet die Tabelle `menschen` (Spalten: `id`, `vorname`, `nachname`, `adresse`, `alter`).
- Unterstützt Befehle im interaktiven Modus oder als CLI-Argumente: `zeigen`, `hinzufuegen`, `loeschen`, `aktualisieren`.

Voraussetzungen
- Java JDK installiert (empfohlen: Java 17 oder neuer).
- Die SQLite JDBC-Treiber-JAR in `lib/sqlite-jdbc.jar`.




