# Datenbank

Kleines Java-Programm das eine einfache SQLite-basierte Datenbank anbietet .

Kurzbeschreibung
- Erzeugt und verwaltet die Tabelle `menschen` (Spalten: `id`, `vorname`, `nachname`, `adresse`, `alter`).
- Unterstützt Befehle im interaktiven Modus : `zeigen`, `hinzufuegen`, `loeschen`, `aktualisieren`.

Voraussetzungen
- Java JDK installiert (empfohlen: Java 17 oder neuer).
- Die SQLite JDBC-Treiber-JAR in `lib/sqlite-jdbc.jar`.




